package Chapter8;

import java.util.Scanner;

public class NewPassword {

    private static Object String;

    public static void main(String[] args) {

        System.out.println("Enter your username");
        Scanner scanner = new Scanner(System.in);
        String name = scanner.next();

        System.out.println("please enter your password");
        Scanner scanner1 = new Scanner(System.in);
        CharSequence oldPassword = scanner1.next();

        System.out.println("please enter a new password");
        Scanner scanner2 = new Scanner(System.in);
        CharSequence newPassword = scanner2.next();

    }
    public static boolean verifyPasswordIsDifferent(boolean newPassword, boolean oldPassword) {

        if( ){
            System.out.println("Your new password must not be the same as the old password");
            return false;
        }
        else {
            return true;

        }


        //public static boolean verifyPasswordIsDifferent (String newPassword, CharSequence oldPassword) {
       //if (newPassword.equals(oldPassword)) {
          //  System.out.println("Your new password must not be the same as the old password");
          //  return false;
        }
        //else System.out.println("Your password has been changed");
        //return true;
    }





        //if password is the same as user name error
        //if password doesn't contain a special character error
        //if password doesn't contain an upper case error
        // if there is a 8 or more character return true
        //error message





        //scanner.close();








